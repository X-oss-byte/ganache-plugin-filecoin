{
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["alpha", "beta", "rc", "main"]
    },
    "bootstrap": {
      "reject-cycles": true
    }
  }
}
